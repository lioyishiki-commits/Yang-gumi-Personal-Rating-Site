# 更新记录

## 1.3.2 - 2026-08-11

- 触发 1.3.1 新更新器的首次完整主分支快照修复，使从旧更新器升级过来的安装也立即清除残留的旧版程序文件。

## 1.3.1 - 2026-08-11

- 修复增量更新只替换 GitHub 差异文件、可能遗留早期版本程序文件的问题；升级时改为核对并修复当前主分支的全部受管理程序文件。
- 全量程序修复仍排除数据库、评分记录、封面、背景、备份、导出、令牌与本机配置，并保留只读分享启动器中的临时链接字段。

## 1.3.0 - 2026-08-11

- Bangumi 新增一次连接、后台加密保存的只读账号模式：只用于补全 R18 动画、漫画、小说与游戏的封面和简介，不读取或导入个人收藏，也不会修改 Bangumi 账号。
- 新增 Bangumi Archive 的 R18 条目补充索引与自动封面回填；缺失官方封面时生成可识别的本地备用封面，新增条目与公开排行榜都能继续显示完整资料。
- 动画、漫画、轻小说、游戏均可独立配置评分框架；漫画、小说和游戏默认关闭季度均分，需要时可手动开启。
- Bangumi 排行搜索与条目库统一为紧凑搜索栏，时间均分默认关闭；账号已连接时隐藏连接提示，仅在断开后重新显示。
- 修正新增条目无匹配结果时页面变灰的问题，直接显示“无相关作品”并允许立即返回或继续搜索。
- 提升全站亮度和文字对比度，整理数据管理页的信息层级、备份恢复、维护工具、外观设置与只读分享排版。
- 修复公网只读分享的失效上游恢复和隧道重连逻辑，访客入口会跟随新的临时链接更新。
- 更新完整配置说明文档；公开版本继续排除数据库、评分、收藏、笔记、封面缓存、背景、令牌、日志、备份、导出和本机路径。

## 1.2.10 - 2026-08-01

- 公网分享在 8501 端口被无响应实例占用时，会自动启动独立只读上游并重建本地安全代理，不再因主站僵死直接停止分享。
- 运行中的本地上游失效时会自动切换到新端口；`localhost.run` 在连续公网健康检查失败后会被回收重连，访客启动器通过稳定定位器获取新链接。
- 本次只更新分享程序、回归测试和版本文档；不包含数据库、评分、收藏、笔记、今日美图、海报、背景、令牌、日志、备份或导出。

## 1.2.9 - 2026-08-01

- 更新器新增写入前的第二层个人内容保护检查；即使以后前置筛选被误改或绕过，只要数据库、评分/收藏记录、今日美图设置、图片索引、本地海报或背景进入更新计划，更新会立即停止，不会写入或删除这些文件。
- 新增真实增量更新回归：在更新程序文件的同时，逐字节确认数据库、收藏封面、今日美图目录设置、图片索引和美图文件全部保持不变。
- 更新控制台会明确显示“0 个个人内容文件进入更新计划”；本次发布仍不包含任何用户数据库、评分、笔记、本机路径或本地图片。

## 1.2.8 - 2026-08-01

- 修复 GitHub 源码 ZIP/Raw 下载保留 LF 换行时，Windows `cmd.exe` 可能从批处理行中间继续解析、导致更新命令被截断的问题；含中文的 `.bat` 现在以 UTF-8 BOM + CRLF、纯英文分享入口以 ASCII + CRLF 原样存入仓库，并增加自动格式回归测试。
- `update_yanggumi.py check` 现在默认安全重启正在运行的网站，不再依赖批处理额外传入重启参数；可用 `--no-restart-running` 显式关闭。
- 继续保护数据库、评分、笔记、图片、备份、导出和私人配置，不上传或覆盖个人数据。

## 1.2.7 - 2026-08-01

- 修复“程序文件已更新，但浏览器仍连接旧版 Streamlit 进程”的问题：手动更新完成后仅识别并重启当前安装目录占用 8501 端口的 Yang-gumi，不结束其他程序，也不额外打开浏览器窗口。
- 更新器在结束进程前同时核对端口、Streamlit 命令行、`app.py` 与当前安装目录；数据库、评分、笔记、海报、背景、备份和导出继续保持只保护不覆盖。
- 从 1.2.6 首次升级到 1.2.7 后，再运行一次新版“检查并更新 Yang-gumi”即可自动重启仍在运行的旧进程；后续版本更新会在同一次更新中自动重启。

## 1.2.6 - 2026-08-01

- 修复网站安装目录与系统临时目录位于不同磁盘时，更新器直接移动暂存文件导致 `[WinError 17]` 并回滚的问题。下载文件现在先复制到目标文件同目录，再执行同盘原子替换。
- 继续包含 1.2.5 的旧季番缓存自动迁移；从 1.2.4 可直接更新到 1.2.6，且不会覆盖数据库、评分、笔记、图片、备份或导出。

## 1.2.5 - 2026-08-01

- 修复同一天先由旧版算法写入季番缓存、随后升级却因“今日已刷新”而继续显示 54 部旧结果的问题。新版本记录季番算法版本；旧缓存即使在当天也会自动重新交叉核对 Bangumi 与 YUC。
- 更新器明确提示升级后需要关闭并重新启动网站；启动时只重建季番公开缓存，继续保留条目状态、评分、笔记和其他个人数据。

## 1.2.4 - 2026-08-01

- 修复虚拟机 GitHub 安装辅助脚本仍下载旧临时分支的问题，统一改为下载官方 `main` 分支，避免全新安装得到过期的新番逻辑。
- 本补丁只修改公开安装脚本、回归测试和版本文档，不包含用户数据库、评分、笔记、图片、备份、导出或其他个人数据。

## 1.2.3 - 2026-08-01

- 本季新番改为每天本地时间 00:00 交叉核对 Bangumi 每日放送与 YUC 当季表；YUC 周播表排除短篇后限定候选，Bangumi 只用于逐部核验和补全，不再并入日历中的额外条目。
- 主海报墙严格只显示日本非短篇 TV 动画；本季实测 70 部，与 YUC 主清单 66 部相差 4。任何季度若刷新结果与 YUC 主清单相差超过 5 部，会拒绝异常刷新并保留旧缓存。
- 新番时光机不受主海报墙限制，继续显示 TV、剧场版、OVA、WEB、SP 等所有动画发行形式。
- 修复 GitHub 空数据安装版因旧匹配缓存导致本季条目不全，以及“全部”状态下混入剧场版的问题；不包含用户数据库、评分、笔记、图片、备份或导出。

## 1.2.2 - 2026-07-30

- 修复评分对比卡片左栏海报未居中的问题，使序号、海报与左栏保持同一中心轴。
- 主站与只读分享站继续共用同一套界面样式；没有改动评分数据、评分逻辑或其他页面排版。

## 1.2.1 - 2026-07-30

- 修复旧更新记录指向已删除临时分支时无法继续升级的问题：只在当前 `VERSION` 能与同版本官方 GitHub 标签核实时，自动改用官方标签重新确认升级路径。
- 无法确认官方标签或官方标签也不在目标分支升级路径时，仍会停止更新，避免误覆盖。
- 更新范围仍只包含网站程序文件；评分数据库、评分记录、私人备注、海报、背景、备份、导出和私人配置继续受到保护。

## 1.2.0 - 2026-07-30

- 新增可持久保存的 Bangumi 搜索分类“固定”按钮；新增条目、Bangumi 排行与公开搜索共用该默认分类，刷新或重新启动后仍保留。
- 评分对比顶部改为紧凑总览，次要筛选折叠收纳，保留整体差异、类型、状态、标签、分项评分、正反排序和每页 24 部等主要功能。
- 主站与只读分享继续共用同一套页面代码；分享站同步采用精简后的评分对比界面。
- 修复只读分享入口首次打开后，点击导航可能变成空白页的问题；六个允许页面可以连续切换。
- 保持更新程序只替换网站程序文件，不覆盖评分数据库、海报、背景、备份、导出、分享数据或私人配置。

## 1.1.1 - 2026-07-30

- 更新检测改为读取 GitHub `VERSION` 的正式版本号；全新 v1.1.0 不会再被误报为不存在的 v1.2.0。
- 没有本地更新记录时，会按当前版本对应的 GitHub 标签建立正确基线，不再固定从 v1.0.0 重复比较。
- 更新和程序回退均不覆盖数据库、评分记录、海报、背景、备份、导出、分享数据或私人配置。
- 更新前仍保留数据库应急快照，但自动失败恢复和手动程序回退只处理程序文件，避免旧快照覆盖后来新增的记录。

## 1.1.0 - 2026-07-29

- “已追番”“已弃番”“已完成”改为互斥状态，避免同一作品同时出现多个追番状态。
- “评分对比”增加按评分小项目升序、降序浏览与单项校准，可将调整后的单项评分应用到整部作品；分项分页默认每页 24 条。
- 主站“数据管理”与只读分享“评分设置”增加完整评分明细 XLSX，包含总评、全部当前评分小项目与评分规则，不包含私人备注和本机路径。
- 只读分享升级为六页面交互式网站，与主站共用实时数据；增加图片转发、临时令牌、动态链接定位、自动重连与多种免账号公网通道。
- 加快主站和分享站加载速度，并补充首次部署、分享、迁移、隐私与依赖说明。

## 2026-07-13

- Bangumi 公开排行改用官方 `/v0/subjects` API，按动画、漫画、小说、游戏分类读取，并升级缓存格式以清除旧网页解析产生的空缓存。
- Bangumi 跳页和跳名次改为直接读取目标区间，封面缓存改为后台执行，避免高页码按顺序补齐前面数千条。
- 今日美图只扫描用户明确选择的本机目录，并继续显示检查、识别、生成和无法读取数量。
- 新增官方排行缓存与中文路径图库的回归测试。

## 2026-07-12

- 将主站启动与只读分享彻底解耦，并增加启动器回归测试。
- 修复旧 Edge 中输入框、数字框、展开栏和上传区出现白边/白框的问题。
- 今日美图支持有上限的多级子目录递归扫描。
- 数据管理增加 `.db` 加载入口；新增可选保存数据库并校验完整性的 Windows 卸载程序。
- 更新新电脑部署说明和 Dark Reader 旧浏览器兜底步骤。

## 2026-07-06

- 发布可独立部署的本地版本。
- 增加 Windows 安装与启动脚本。
- 补充旧版 Chromium Edge 的前端兼容处理。
- 完善数据目录隔离、隐私说明和迁移文档。
- 修正页面在浏览器缩放时的卡片布局。

# Contributing

欢迎提交问题和改进。开始前请：

1. 从 `main` 创建分支。
2. 不提交真实 `data/`、备份、图片缓存、访问令牌或本机绝对路径。
3. 运行 `python -m unittest discover -s tests -v`。
4. 在 PR 中说明用户可见变化、验证方式与隐私影响。

界面文本以简体中文为主；代码、配置键与提交信息尽量保持清楚、稳定、便于检索。
